<nav id="navhome" class="navbar navbar-expand-lg navbar-light fixed-top">
    <a class="navbar-brand" href="#">
        <img src="{{ asset('img/library/hbtlogo.png') }}" width="32" height="32" class="d-inline-block align-top" alt="">
        HappyBizTrip
    </a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
        <div class="navbar-nav">
            <a class="nav-item nav-link active" href="#">{% trans %}Home{% endtrans %} <span class="sr-only">(current)</span></a>
            <a class="nav-item nav-link" href="#">{% trans %}Concept{% endtrans %}</a>
            <a class="nav-item nav-link" href="#">{% trans %}Pricing{% endtrans %}</a>
            <a class="nav-item nav-link" href="#">{{ locale }}</a>
        </div>
        <ul class="navbar-nav ml-auto mr-4">
            <li class="nav-item dropdown">
                <a href="#" class="nav-link dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false" aria-haspopup="true" id="locales">
                    <i class="fa fa-globe"></i> {{ app.request.locale }}
                </a>
                <div class="dropdown-menu dropdown-menu-center" aria-labelledby="locales">
                    {% for locale in locales() %}
                        {% if app.request.locale != locale.code %}
                            <a class="dropdown-item" href="{{ path(app.request.get('_route', 'blog_index'), app.request.get('_route_params', [])|merge({_locale: locale.code})) }}">{{ locale.name|capitalize }} <small>{{ locale.code[0:2] }}</small></a>
                        {% endif %}
                    {% endfor %}
                </div>
            </li>
        </ul>
        <a href="#">
            <button class="btn btn-outline-happyDarkBlue btn-sm my-2 my-sm-0">{% trans %}Sign in{% endtrans %}</button>
        </a>
    </div>
</nav>