{% extends 'base.html.twig' %}

{% block title %}{{ 'visit.block_title'|trans|raw }}{% endblock %}

{% block stylesheets %}
    {{ parent() }}    
    {{ encore_entry_link_tags('css/search') }}
{% endblock %}

{% block body %}
<div class="container">
    <div class="row pt-4">
        <div class="col">
            <span class="text-result float-left">{{ 'visit.result'|trans({count: paginator|length })|raw }}</span>
        </div>
    </div>
    <hr>
    {# diplay the list of profiles - variable list = V = Visit #}
    {% include 'partials/_listProfile.html.twig'  with {'route_name' : 'show_visit', 'list' : 'V'} %}   
</div>

{% endblock %}

{% block javascripts %}
    {{ parent() }}
    {% include 'partials/js/listProfileJs.html.twig' %}
{% endblock %}
